{
  "buildCommand": "cd clients && npm run build",
  "outputDirectory": "clients/dist",
  "installCommand": "cd clients && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
} 